# 朱萌个人网站

Astro + GSAP 的中文个人 Portfolio 第一版。

## 开发

```bash
npm run dev
```

## 构建

```bash
npm run build
```

项目内容位于 `src/data/projects.js`，详情页使用 `/projects/[slug]` 动态生成。个人 IP 形象完成后，将首屏的 `.hero-art` 替换为真实视觉素材即可。
